extends default

block content
  h3 Price tracker
  p Usa comma separated values to query assets (i.e bitcoin,cosmos,ethereum):
  p Link to complete list: 
  a(href='https://docs.google.com/spreadsheets/d/1wTTuxXt8n9q7C4NDXqQpI3wpKu1_5bGVmP9Xz0XGSyU/edit#gid=0') Coins

  p
  form(action='/prices' method='GET')
      input.search-input(autofocus='' value=`${searchQuery || ''}` placeholder='Enter a search term' type='search' name='q')

  ul
    each v, index in results
      li= index + ': ' + v.usd
